#!/bin/sh
# live-build auto/clean — invoked by `lb clean`.

set -eu

lb clean noauto "${@}"
# Match the --image-name (anglerfish-ai), not the live-build default name,
# so a prior run's ISO is genuinely purged.
rm -rf binary chroot cache anglerfish-ai-*.hybrid.iso
